<% if @current_user && @current_user.id == @user.id %>
<div id="page_wrapper">
  <h1>Editing Post</h1>

  <%= render 'form', post: @post %>

  <%= link_to 'Show', @post %> |
  <%= link_to 'Back', posts_path %>
</div>
<% else redirect_to '/' %>
<% end %>
